/public/
/resources/_gen/
/assets/jsconfig.json
hugo_stats.json
.hugo_build.lock

# Pagefind index (generated at build/deploy time)
/static/pagefind/

# Node (used only for local tooling, e.g. pagefind CLI via npx)
node_modules/

# OS / editor
.DS_Store
*.swp

# Decap CMS local backend
.netlify/
